apiVersion: 1

providers:
  - name: 'gsw-dashboard-provider'
    folder: GSW
    type: file
    updateIntervalSeconds: 10
    allowUiUpdates: true

    options:
      path: /etc/grafana/provisioning/gsw-dashboards
